'use strict';

jest.mock('fs');

const reader = require('../app.js');

describe('File Reader', () => {
  xit('it should return the content of the file in UPPERCASE', done => {
    // let file = `../test.txt`;
    // reader(file, data) => {
    // })
  });
});
